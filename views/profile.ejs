<!doctype html>
<html>
<head>
    <title>21 Savage Fan Site</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
    <link rel="stylesheet" href="style.css">
</head>
<body>
<div class="container">

    <div class="page-header text-center">
        <h1><span class="fa fa-anchor"></span> <%= user.local.email%>'s' Launch Page</h1>
        <a href="/logout" class="btn btn-default btn-sm">Logout</a>
    </div>

    <!-- <div class="row">

        LOCAL INFORMATION
        <div class="col-sm-6">
            <div class="well">
                <h3><span class="fa fa-user"></span> Local</h3>

                <% if (user.local.email) { %>
                    <p>
                        <strong>id</strong>: <%= user._id %><br>
                        <strong>email</strong>: <%= user.local.email %><br>
                        <strong>password</strong>: <%= user.local.password %>
                    </p>

                    <a href="/unlink/local" class="btn btn-default">Unlink</a>
                <% } else { %>
                    <a href="/connect/local" class="btn btn-default">Connect Local</a>
                <% } %>

            </div>
        </div> -->
        <div class="col-sm-6">
          <h3>Upcoming Bills</h3>
          
              <span class="label">Company</span> 
              <span class="label">Due Date</span>
              <span class="label">Payment Amount</span>
          
    
          <ul class="messages">
          <% for(var i=0; i < data.length; i++) {%>
           <% if(data[i].name === user.local.email ) {%>
            <li class="message">
              <span><%= data[i].company %></span>
              <span><%= data[i].date %></span>
              <span><%= data[i].amt %></span>
              <button class ="paidBtn" type ="button">Paid</button> 
              <button class ="trashBtn" type ="button">Trash</button> 
            </li>
            <% } %>
          <% } %>
          </ul>

          <h2>Enter new bills</h2>
        <% if (user.local.email) { %>
          <form action="/list" method="POST">
            <label for="company"></label>
            <input type="text" placeholder="Company Name" name="company">
            <label for="date"></label>
            <input type="text" placeholder="Due Date" name="date">
            <label for="amt"></label>
            <input type="text" placeholder="$200"  name ="amt">
            <button type="submit">Submit</button>
          </form>
        </div>
        <% } %>

</div>

<script src="main.js"></script>

</body>
</html>
